# Local deployment inputs for docker-socks5-deploy.sh.
# The script copies these values into generated .env.
SUB2API_IMAGE=ghcr.io/0990sub2api/sub2api-socks5:v0.0.3
SERVER_PORT=8080
FORCED_OPENAI_OAUTH_SOCKS5_HOST=localhost
FORCED_OPENAI_OAUTH_SOCKS5_PORT=1080
FORCED_OPENAI_OAUTH_SOCKS5_PASSWORD=123456
